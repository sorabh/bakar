<%= content_for :stylesheets do %><%= stylesheet_link_tag 'home.css' %><%- end %>

<%= render '/shared/header' %>


<section id="hero" class="hero">
  <h1>Bring your interest together </h1>
  <p> chat anonymously with people of common interest </p>
  <div>
    <% if current_user %>
        <a href="/request_chat" class="button green" >Start chatting, it's free</a>
    <% else %>
      <a href="/auth/facebook" class="button green", id=sign_in">Start chatting, it's free</a>
    <% end %>
  </div>
</section>